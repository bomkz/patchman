{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build & Debug GUI",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${workspaceFolder}/debug.exe",
            "preLaunchTask": "build-gui"
        }
    ]
}
